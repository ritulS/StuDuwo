apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SDChart.fullname" . }}-test-connection"
  labels:
    {{- include "SDChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "SDChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
