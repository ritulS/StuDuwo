apiVersion: v1
kind: Service
metadata:
  name: {{ include "SDChart.fullname" . }}
  labels:
    {{- include "SDChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "SDChart.selectorLabels" . | nindent 4 }}
